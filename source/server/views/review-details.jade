include layout
block content
    div.container
        h1= data.title
        p= data.content
        p Rating
            span.badge
                =data.rating
        if data.images.length > 0
            img.img-responsive(styles="width: 100%", src="../" + data.images[0])